function townInfo(town,population,area) {
        
    let result = `Town ${town} has population of ${population} and area ${area} square km.`;

    console.log(result);
}
townInfo('Sofia',1286383,492)